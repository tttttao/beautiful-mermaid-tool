## 2024-05-20 - Add Copy to Clipboard in MermaidEditor\n**Learning:** Added `aria-live="polite"` to dynamically changing icons (like a copy success checkmark) effectively communicates state changes to screen readers without shifting keyboard focus.\n**Action:** Use `aria-live` on containers or interactive elements that temporarily change their visual/iconic state to reflect a success/error status.
